services:
    frontend:
        build:
            context: ./frontend
            dockerfile: Dockerfile
        ports:
            - "3000:3000"
        environment:
            - NODE_ENV=production
            - NUXT_PUBLIC_SITE_URL=https://me.fbufler.cloud
        restart: always
